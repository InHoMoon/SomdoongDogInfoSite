<?xml version="1.0" encoding="UTF-8"?>
<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    

<mapper namespace="somdoong.mypage.dao.face.InquireDao">   	
 <select id="selectCntAll" resultType="int">
		SELECT count(*) FROM inquire
	</select>
    
    <select id="selectList" resultType="Inquire" parameterType="Paging">
		SELECT * FROM (
			SELECT rownum rnum, I.* FROM (
				SELECT
					i_num, i_title, i_content , i_category,i_writedate
				FROM inquire
				ORDER BY i_num DESC
			) I
		) INQUIRE
		WHERE rnum BETWEEN #{startNo } AND #{endNo }
	</select>
	
</mapper>